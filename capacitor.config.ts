import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-cowsay4k',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
